services:
  db:
    image: postgres:15-alpine
    container_name: pms_postgres_db_dev
    environment:
      POSTGRES_DB: PMS_Sys
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d PMS_Sys"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: pms_redis_cache_dev
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD:?REDIS_PASSWORD is required}
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "${REDIS_PASSWORD:?REDIS_PASSWORD is required}"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data_dev:/data
    healthcheck:
      test: ["CMD-SHELL", "REDISCLI_AUTH=$${REDIS_PASSWORD} redis-cli ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build:
      context: ../..
      dockerfile: DevOps/docker/Dockerfile.backend
    container_name: pms_fastapi_app_dev
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/PMS_Sys
      - REDIS_URL=redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
      - REDIS_SOCKET_TIMEOUT_SECONDS=${REDIS_SOCKET_TIMEOUT_SECONDS:-0.25}
      - REDIS_RETRY_INTERVAL_SECONDS=${REDIS_RETRY_INTERVAL_SECONDS:-30}
      - PMS_REALTIME_MODE=${PMS_REALTIME_MODE:-in_process}
      - PMS_ASYNC_JOBS_ENABLED=${PMS_ASYNC_JOBS_ENABLED:-false}
      - PMS_JOB_POLL_SECONDS=${PMS_JOB_POLL_SECONDS:-1}
      - PMS_JOB_LEASE_SECONDS=${PMS_JOB_LEASE_SECONDS:-300}
      - PMS_JOB_MAX_ATTEMPTS=${PMS_JOB_MAX_ATTEMPTS:-3}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ALGORITHM=HS256
      - JWT_EXPIRE_MINUTES=1440
      - PMS_DATA_DIR=/app/data
      - PMS_DEFAULT_FILE_PATH=/app/data/PMS_Trend_All.xlsx
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
      - MAX_UPLOAD_BYTES=${MAX_UPLOAD_BYTES:-26214400}
      - PORT=8000
    volumes:
      - backend_data_dev:/app/data
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  worker:
    build:
      context: ../..
      dockerfile: DevOps/docker/Dockerfile.backend
    command: ["sh", "-c", "exec python worker.py"]
    environment:
      - APP_ENV=development
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/PMS_Sys
      - REDIS_URL=redis://:${REDIS_PASSWORD:?REDIS_PASSWORD is required}@redis:6379/0
      - REDIS_SOCKET_TIMEOUT_SECONDS=${REDIS_SOCKET_TIMEOUT_SECONDS:-0.25}
      - REDIS_RETRY_INTERVAL_SECONDS=${REDIS_RETRY_INTERVAL_SECONDS:-30}
      - PMS_ASYNC_JOBS_ENABLED=${PMS_ASYNC_JOBS_ENABLED:-false}
      - PMS_JOB_POLL_SECONDS=${PMS_JOB_POLL_SECONDS:-1}
      - PMS_JOB_LEASE_SECONDS=${PMS_JOB_LEASE_SECONDS:-300}
      - PMS_JOB_MAX_ATTEMPTS=${PMS_JOB_MAX_ATTEMPTS:-3}
      - PMS_DATA_DIR=/app/data
      - PMS_DEFAULT_FILE_PATH=/app/data/PMS_Trend_All.xlsx
      - JWT_SECRET=${JWT_SECRET}
      - JWT_ALGORITHM=HS256
      - JWT_EXPIRE_MINUTES=1440
    volumes:
      - backend_data_dev:/app/data
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  postgres_data_dev:
  redis_data_dev:
  backend_data_dev:
